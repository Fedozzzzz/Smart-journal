import React,{Component} from "react"
import {connect} from "react-redux";
import {bindActionCreators} from "redux";

class AttendanceAndPayments extends Component{

    render() {
        return(<div>

        </div>)
    }
}

export default connect()(AttendanceAndPayments)